const arr1 = [3, 4, 2, 5];

function previous(arr) {
    let newArr = [];
    let first = arr[0];
    for (let i = 1; i < arr.length; i++) {
        let second = arr[0] + arr[i];
    }
    
    return newArr;
}

console.log(previous(arr1));
